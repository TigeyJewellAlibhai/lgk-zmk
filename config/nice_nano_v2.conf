# Copyright (c) 2025
# SPDX-License-Identifier: MIT

# Board-specific configuration for nice!nano v2

# CRITICAL: Disable NFC pins - P0.09 and P0.10 are used for matrix
CONFIG_NFCT_PINS_AS_GPIOS=y

# Enable USB (should be default, but explicitly setting)
CONFIG_USB_DEVICE_STACK=y
CONFIG_ZMK_USB=y
